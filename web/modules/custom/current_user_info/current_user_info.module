<?php

function current_user_info_theme($existing, $type, $theme, $path) {
   return [
    'current_user_info_block' => [
      'variables' => [
        'name' => '',
        'picture_uri' => '',
        'current_user' => '',
      ],
    ],
  ];
}

function current_user_info_link_alter(&$variables) {
  if ($variables['text'] == 'My account') {
    $variables['text'] = t('My Profile');
  }
}